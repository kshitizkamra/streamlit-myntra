# Streamlit Myntra Processor

Upload a CSV with a `Product_ID` column. The app will fetch image URL, selling price, MRP and discount label for each ID.

Use the sidebar to limit rows and concurrency. Start with small files (10–30 rows) to confirm behavior.
